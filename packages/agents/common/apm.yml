---
# APM sub-package: harness-neutral primitives. No targets restriction,
# so its content deploys under whichever targets a consumer activates.
# Consumers pin it as tbhb/repotools/packages/agents/common#vX.Y.Z; the
# repo root manifest depends on it by local path. The cog pre-bump hook
# keeps `version` in lockstep with the root (see cog.toml).
name: repotools-agents-common
version: 0.7.1
description: Harness-neutral agent primitives for tbhb repositories
author: tbhb
license: Apache-2.0
